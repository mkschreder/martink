#include "serial.h"
#include <kernel/list.h>

//static LIST_HEAD(_devices); 
//struct serial_device *_default_system_console = 0; 

/*void register_serial_device(struct serial_device *dev){
	if(!_default_system_console) _default_system_console = dev; 
	list_add_tail(&dev->list, &_devices); 
}*/

